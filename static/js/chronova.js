document.addEventListener('DOMContentLoaded',()=>{const nav=document.querySelector('[data-nav-toggle]'),links=document.querySelector('[data-nav-links]');nav?.addEventListener('click',()=>links.classList.toggle('open'));document.querySelectorAll('[data-modal-open]').forEach(button=>button.addEventListener('click',()=>{const modal=document.getElementById(button.dataset.modalOpen);modal?.classList.add('open');modal?.setAttribute('aria-hidden','false')}));document.querySelectorAll('[data-modal-close]').forEach(button=>button.addEventListener('click',()=>button.closest('.modal').classList.remove('open')));document.querySelectorAll('.modal').forEach(modal=>modal.addEventListener('click',event=>{if(event.target===modal)modal.classList.remove('open')}));document.querySelectorAll('[data-dismiss-alert]').forEach(button=>button.addEventListener('click',()=>button.parentElement.remove()));document.querySelectorAll('[data-confirm]').forEach(form=>form.addEventListener('submit',event=>{if(!confirm(form.dataset.confirm))event.preventDefault()}));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.querySelectorAll('[data-count]').forEach(el=>{const count=()=>{const target=Number(el.dataset.count),start=performance.now(),duration=1100;const tick=now=>{el.textContent=Math.min(target,Math.floor((now-start)/duration*target));if(now-start<duration)requestAnimationFrame(tick);else el.textContent=target};requestAnimationFrame(tick)};const counter=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){count();counter.disconnect()}}));counter.observe(el)});const week=document.querySelector('[data-week-view]'),daily=document.querySelector('[data-daily-view]');document.querySelectorAll('[data-schedule-view]').forEach(button=>button.addEventListener('click',()=>{document.querySelectorAll('[data-schedule-view]').forEach(b=>b.classList.remove('active'));button.classList.add('active');const showDaily=button.dataset.scheduleView==='daily';if(week)week.hidden=showDaily;if(daily)daily.hidden=!showDaily}));document.addEventListener('mousemove',event=>{const orb=document.querySelector('.orb-one');if(orb&&window.innerWidth>800){orb.style.transform=`translate(${event.clientX*.012}px,${event.clientY*.012}px)`}})});
